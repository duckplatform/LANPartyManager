<%- include('../partials/head') %>

<style>
  .battle-status-badge {
    display: inline-flex;
    align-items: center;
    gap: .35rem;
    padding: .35rem .65rem;
    border-radius: 1rem;
    box-sizing: border-box;
    font-size: .8rem;
    font-weight: 600;
    line-height: 1.2;
    min-height: 2rem;
    white-space: nowrap;
  }
  .battle-status--live      { background: #E8F5E9; color: #388E3C; }
  .battle-status--planned   { background: #E3F2FD; color: #1976D2; }
  .battle-status--queue     { background: #FFF8E1; color: #F57F17; }
  .battle-status--install   { background: #F3E5F5; color: #7B1FA2; }
  .battle-status--done      { background: #F5F5F5; color: #757575; }
  .ranking-row-hover:hover  { background-color: rgba(0,0,0,0.02); }
  .stat-card {
    background: var(--color-surface, #fff);
    border: 1px solid rgba(0,0,0,.07);
    border-radius: var(--radius-md, .75rem);
    padding: var(--space-md, 1rem) var(--space-lg, 1.5rem);
    text-align: center;
    flex: 1;
    min-width: 130px;
  }
  .stat-card .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-primary, #5865F2);
    line-height: 1;
    margin-bottom: .25rem;
  }
  .stat-card .stat-label {
    font-size: .8rem;
    color: var(--color-text-muted, #888);
    text-transform: uppercase;
    letter-spacing: .05em;
  }
  .event-details-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }
  .event-section-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }
  .event-ranking-card {
    max-width: 760px;
  }
  .event-charts-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }
  .event-chart-card {
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    min-height: 100%;
  }
  .event-chart-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: .75rem;
  }
  .event-chart-title {
    margin: 0;
    font-size: 1rem;
    display: flex;
    align-items: center;
    gap: .5rem;
  }
  .event-chart-subtitle {
    margin: .2rem 0 0;
    font-size: .82rem;
    color: var(--color-text-muted);
  }
  .chart-kicker {
    padding: .3rem .6rem;
    border-radius: 999px;
    background: rgba(88, 101, 242, 0.1);
    color: var(--color-primary, #5865F2);
    font-size: .74rem;
    font-weight: 700;
    white-space: nowrap;
  }
  .chart-donut-layout {
    display: grid;
    grid-template-columns: 180px minmax(0, 1fr);
    gap: var(--space-md);
    align-items: center;
  }
  .chart-donut {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    position: relative;
    margin: 0 auto;
  }
  .chart-donut::after {
    content: '';
    position: absolute;
    inset: 20px;
    border-radius: 50%;
    background: var(--color-bg-card);
    box-shadow: inset 0 0 0 1px rgba(0,0,0,.05);
  }
  .chart-donut-center {
    position: absolute;
    inset: 0;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 0 1.2rem;
  }
  .chart-donut-value {
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
    color: var(--color-text);
  }
  .chart-donut-label {
    margin-top: .3rem;
    font-size: .78rem;
    color: var(--color-text-muted);
    line-height: 1.3;
  }
  .chart-legend {
    display: grid;
    gap: .55rem;
  }
  .chart-legend-item {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: .6rem;
    padding: .55rem .7rem;
    border-radius: var(--radius-md, .75rem);
    background: rgba(0,0,0,.02);
  }
  .chart-legend-swatch {
    width: .8rem;
    height: .8rem;
    border-radius: 999px;
  }
  .chart-legend-label {
    font-size: .88rem;
    color: var(--color-text);
  }
  .chart-legend-value {
    font-size: .82rem;
    color: var(--color-text-muted);
    white-space: nowrap;
  }
  .chart-bars {
    display: grid;
    gap: .8rem;
  }
  .chart-bar-item {
    display: grid;
    gap: .35rem;
  }
  .chart-bar-topline {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: .75rem;
  }
  .chart-bar-label {
    font-size: .92rem;
    font-weight: 600;
    color: var(--color-text);
  }
  .chart-bar-meta {
    font-size: .78rem;
    color: var(--color-text-muted);
    white-space: nowrap;
  }
  .chart-bar-track {
    position: relative;
    height: .75rem;
    border-radius: 999px;
    overflow: hidden;
    background: rgba(15, 23, 42, 0.08);
  }
  .chart-bar-fill {
    height: 100%;
    border-radius: inherit;
  }
  .chart-note {
    font-size: .78rem;
    color: var(--color-text-muted);
  }
  .chart-empty {
    padding: var(--space-md);
    border-radius: var(--radius-md, .75rem);
    background: rgba(0,0,0,.02);
    color: var(--color-text-muted);
    font-size: .88rem;
  }
  .event-battles-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-height: 720px;
    overflow-y: auto;
    padding-right: .35rem;
  }
  .event-battle-card {
    padding: var(--space-lg);
    overflow: visible;
  }
  .event-battle-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .event-battle-body {
    flex: 1;
    min-width: 0;
  }
  .event-battle-meta {
    font-size: .82rem;
    color: var(--color-text-muted);
    line-height: 1.45;
  }
  .event-battle-players {
    margin-top: .5rem;
    font-size: .95rem;
    color: var(--color-text-muted);
    line-height: 1.7;
  }
  .event-battle-room {
    font-size: .82rem;
    color: var(--color-text-muted);
    text-align: right;
    line-height: 1.4;
  }
  @media (max-width: 768px) {
    .event-charts-grid {
      grid-template-columns: 1fr;
    }
    .chart-donut-layout {
      grid-template-columns: 1fr;
    }
    .event-battle-card {
      padding: var(--space-md);
    }
    .event-battles-list {
      max-height: none;
      overflow: visible;
      padding-right: 0;
    }
  }
</style>

<div class="container" style="padding-top: var(--space-xl); padding-bottom: 8rem; min-height: 80vh;">

  <!-- Fil d'Ariane -->
  <nav aria-label="breadcrumb" style="margin-bottom: var(--space-md); font-size: .85rem; color: var(--color-text-muted);">
    <a href="/events" style="color: var(--color-primary);">
      <i class="fas fa-calendar-days"></i> <%= t('event.events') %>
    </a>
    <span style="margin: 0 .4rem;">/</span>
    <span><%= event.name %></span>
  </nav>

  <%
    const dateObj    = new Date(event.start_at);
    const dateFr     = dateObj.toLocaleDateString(locale, { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' });
    const timeFr     = dateObj.toLocaleTimeString(locale, { hour: '2-digit', minute: '2-digit' });
    const isLive     = event.status === 'in_progress';
    const isDone     = event.status === 'ended';
    const deadlineISO = dateObj.toISOString();
    const totalBattles   = battles.length;
    const doneBattles    = battles.filter(b => b.status === 'ended').length;
    const activeBattles  = battles.filter(b => ['in_progress', 'setup'].includes(b.status)).length;
    const statusChart = chartData && chartData.statuses ? chartData.statuses : { total: 0, segments: [], gradient: 'conic-gradient(#E5E7EB 0 100%)' };
    const gameChart = chartData && chartData.games ? chartData.games : { max: 0, items: [] };
    const rankingChart = chartData && chartData.rankings ? chartData.rankings : { max: 0, items: [] };
    const roomChart = chartData && chartData.rooms ? chartData.rooms : { max: 0, total: 0, activeNow: 0, availableNow: 0, items: [] };
  %>

  <!-- En-tête événement -->
  <div class="card" style="margin-bottom: var(--space-lg);">
    <div class="card-body">
      <div style="display:flex; align-items:flex-start; justify-content:space-between; flex-wrap:wrap; gap: var(--space-sm);">

        <div style="flex:1; min-width:200px;">
          <!-- Statut -->
          <div style="margin-bottom: var(--space-sm);">
            <% if (isLive) { %>
              <span class="event-status-badge event-status--live">
                <i class="fas fa-circle-play"></i> <%= t('status.in_progress') %>
              </span>
            <% } else if (isDone) { %>
              <span class="event-status-badge" style="background:#3c3f45;color:#fff;">
                <i class="fas fa-flag-checkered"></i> <%= t('status.ended') %>
              </span>
            <% } else { %>
              <span class="event-status-badge event-status--upcoming">
                <i class="fas fa-calendar-check"></i> <%= t('status.planned') %>
              </span>
            <% } %>
          </div>

          <h1 style="font-size:1.6rem; margin:0 0 var(--space-sm) 0;">
            <i class="fas fa-gamepad"></i> <%= event.name %>
          </h1>

          <ul style="list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:.4rem; color: var(--color-text-muted); font-size:.9rem;">
            <li><i class="fas fa-calendar-alt" style="width:1.2rem;"></i> <%= dateFr %> <%= t('common.at') %> <%= timeFr %></li>
            <li><i class="fas fa-location-dot" style="width:1.2rem;"></i> <%= event.location %></li>
          </ul>
        </div>

        <!-- Actions -->
        <div style="display:flex; flex-direction:column; gap:.5rem; align-items:flex-end;">
          <% if (!currentUser && event.registrationOpen) { %>
            <a href="/auth/login?redirect=/events/<%= event.id %>" class="btn btn--primary btn--sm">
              <i class="fas fa-right-to-bracket"></i> <%= t('event.login_to_register') %>
            </a>
          <% } else if (!currentUser) { %>
            <span class="text-muted" style="font-size:.85rem; display:flex; align-items:center; gap:.4rem;">
              <i class="fas fa-lock"></i> <%= t('event.registrations_closed') %>
            </span>
          <% } else if (isRegistered) { %>
            <div class="event-registered-badge" style="font-size:.9rem;">
              <i class="fas fa-circle-check"></i> <%= t('event.registration_confirmed') %>
            </div>
            <% if (event.registrationOpen) { %>
              <form action="/events/<%= event.id %>/unregister" method="POST" style="display:inline;">
                <input type="hidden" name="_csrf" value="<%= csrfToken() %>" />
                <button type="submit" class="btn btn--ghost btn--xs"
                        onclick="return confirm('<%= t('event.confirm_unregister_named', { name: event.name }) %>')">
                  <i class="fas fa-user-minus"></i> <%= t('event.unregister') %>
                </button>
              </form>
            <% } %>
            <a href="/profile" class="btn btn--ghost btn--xs" title="<%= t('event.my_ticket') %>">
              <i class="fas fa-ticket"></i> <%= t('event.my_ticket') %>
            </a>
          <% } else if (event.registrationOpen) { %>
            <form action="/events/<%= event.id %>/register" method="POST" style="display:inline;">
              <input type="hidden" name="_csrf" value="<%= csrfToken() %>" />
              <button type="submit" class="btn btn--primary btn--sm">
                <i class="fas fa-user-plus"></i> <%= t('event.register') %>
              </button>
            </form>
          <% } else { %>
            <span class="text-muted" style="font-size:.85rem; display:flex; align-items:center; gap:.4rem;">
              <i class="fas fa-lock"></i> <%= t('event.registrations_closed') %>
            </span>
          <% } %>
          <a href="/events" class="btn btn--ghost btn--xs">
            <i class="fas fa-arrow-left"></i> <%= t('event.back_to_events') %>
          </a>
        </div>

      </div>

      <!-- Compte à rebours si inscriptions ouvertes -->
      <% if (event.registrationOpen && !isLive) { %>
        <div class="event-countdown-wrap" style="margin-top: var(--space-md);">
          <p class="event-countdown-label" style="font-size:.8rem;">
            <i class="fas fa-hourglass-half"></i> <%= t('event.registrations_open_until') %>
          </p>
          <div class="event-countdown event-countdown--sm" data-deadline="<%= deadlineISO %>">
            <div class="countdown-unit"><span class="countdown-num">--</span><span class="countdown-lbl"><%= t('countdown.short_days') %></span></div>
            <span class="countdown-sep">:</span>
            <div class="countdown-unit"><span class="countdown-num">--</span><span class="countdown-lbl"><%= t('countdown.short_hours') %></span></div>
            <span class="countdown-sep">:</span>
            <div class="countdown-unit"><span class="countdown-num">--</span><span class="countdown-lbl"><%= t('countdown.minutes') %></span></div>
            <span class="countdown-sep">:</span>
            <div class="countdown-unit"><span class="countdown-num">--</span><span class="countdown-lbl"><%= t('countdown.short_seconds') %></span></div>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Statistiques générales -->
  <div style="display:flex; flex-wrap:wrap; gap: var(--space-md); margin-bottom: var(--space-lg);">
    <div class="stat-card">
      <div class="stat-value"><%= registrationCount %></div>
      <div class="stat-label"><i class="fas fa-users"></i> <%= registrationCount !== 1 ? t('event.registered_plural') : t('event.registered_singular') %></div>
    </div>
    <div class="stat-card">
      <div class="stat-value"><%= rankings.length %></div>
      <div class="stat-label"><i class="fas fa-ranking-star"></i> <%= rankings.length !== 1 ? t('event.ranked_plural') : t('event.ranked_singular') %></div>
    </div>
    <div class="stat-card">
      <div class="stat-value"><%= totalBattles %></div>
      <div class="stat-label"><i class="fas fa-sword"></i> <%= totalBattles !== 1 ? t('battle.matches_plural') : t('battle.matches_singular') %></div>
    </div>
    <% if (totalBattles > 0) { %>
    <div class="stat-card">
      <div class="stat-value"><%= doneBattles %></div>
      <div class="stat-label"><i class="fas fa-flag-checkered"></i> <%= doneBattles !== 1 ? t('event.finished_plural') : t('event.finished_singular') %></div>
    </div>
    <% if (activeBattles > 0) { %>
    <div class="stat-card">
      <div class="stat-value" style="color: #388E3C;"><%= activeBattles %></div>
      <div class="stat-label"><i class="fas fa-bolt"></i> <%= t('status.in_progress') %></div>
    </div>
    <% } %>
    <% } %>
  </div>

  <!-- Graphiques -->
  <div class="event-charts-grid">
    <section class="card event-chart-card">
      <div class="event-chart-head">
        <div>
          <h2 class="event-chart-title"><i class="fas fa-chart-pie"></i> <%= t('event.status_breakdown') %></h2>
          <p class="event-chart-subtitle"><%= t('event.status_breakdown_subtitle') %></p>
        </div>
        <span class="chart-kicker"><%= statusChart.total %> <%= statusChart.total !== 1 ? t('battle.matches_plural') : t('battle.matches_singular') %></span>
      </div>

      <% if (statusChart.total === 0) { %>
        <div class="chart-empty"><%= t('event.chart_status_empty') %></div>
      <% } else { %>
        <div class="chart-donut-layout">
          <div class="chart-donut" style="background:<%= statusChart.gradient %>;">
            <div class="chart-donut-center">
              <div class="chart-donut-value"><%= doneBattles %></div>
              <div class="chart-donut-label"><%= doneBattles !== 1 ? t('event.finished_plural') : t('event.finished_singular') %></div>
            </div>
          </div>

          <div class="chart-legend">
            <% statusChart.segments.forEach(function(segment) { %>
              <div class="chart-legend-item">
                <span class="chart-legend-swatch" style="background:<%= segment.color %>;"></span>
                <span class="chart-legend-label"><%= segment.label %></span>
                <span class="chart-legend-value"><%= segment.count %> · <%= segment.percent %>%</span>
              </div>
            <% }); %>
          </div>
        </div>
      <% } %>
    </section>

    <section class="card event-chart-card">
      <div class="event-chart-head">
        <div>
          <h2 class="event-chart-title"><i class="fas fa-gamepad"></i> <%= t('event.matches_by_game') %></h2>
          <p class="event-chart-subtitle"><%= t('event.matches_by_game_subtitle') %></p>
        </div>
        <span class="chart-kicker"><%= gameChart.items.length %> jeu<%= gameChart.items.length !== 1 ? 'x' : '' %></span>
      </div>

      <% if (gameChart.items.length === 0) { %>
        <div class="chart-empty"><%= t('event.chart_games_empty') %></div>
      <% } else { %>
        <div class="chart-bars">
          <% gameChart.items.forEach(function(item) { %>
            <div class="chart-bar-item">
              <div class="chart-bar-topline">
                <span class="chart-bar-label"><%= item.name %></span>
                <span class="chart-bar-meta"><%= item.total %> <%= item.total !== 1 ? t('battle.matches_plural') : t('battle.matches_singular') %> · <%= item.done %> <%= item.done !== 1 ? t('event.finished_plural') : t('event.finished_singular') %></span>
              </div>
              <div class="chart-note"><%= item.console || t('event.console_not_set') %><% if (item.active > 0) { %> · <%= item.active %> <%= item.active !== 1 ? t('event.active_plural') : t('event.active_singular') %><% } %></div>
              <div class="chart-bar-track">
                <div class="chart-bar-fill" style="width:<%= item.width %>%; background:linear-gradient(90deg, #1976D2, #64B5F6);"></div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </section>

    <section class="card event-chart-card">
      <div class="event-chart-head">
        <div>
          <h2 class="event-chart-title"><i class="fas fa-ranking-star"></i> <%= t('event.top_ranking') %></h2>
          <p class="event-chart-subtitle"><%= t('event.top_ranking_subtitle') %></p>
        </div>
        <span class="chart-kicker"><%= rankingChart.items.length %> <%= rankingChart.items.length !== 1 ? t('event.players_plural') : t('event.players_singular') %></span>
      </div>

      <% if (rankingChart.items.length === 0) { %>
        <div class="chart-empty"><%= t('event.chart_ranking_empty') %></div>
      <% } else { %>
        <div class="chart-bars">
          <% rankingChart.items.forEach(function(item) { %>
            <div class="chart-bar-item">
              <div class="chart-bar-topline">
                <span class="chart-bar-label">#<%= item.rang %> · <%= item.username %></span>
                <span class="chart-bar-meta"><%= item.points %> <%= t('common.points') %> · <%= item.wins %> <%= item.wins !== 1 ? t('common.wins_plural') : t('common.wins') %></span>
              </div>
              <div class="chart-note"><%= item.battles_played %> <%= item.battles_played !== 1 ? t('event.played_matches_plural') : t('event.played_matches_singular') %></div>
              <div class="chart-bar-track">
                <div class="chart-bar-fill" style="width:<%= item.width %>%; background:linear-gradient(90deg, #FFB300, #FFD54F);"></div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </section>

    <section class="card event-chart-card">
      <div class="event-chart-head">
        <div>
          <h2 class="event-chart-title"><i class="fas fa-door-open"></i> <%= t('event.rooms_load') %></h2>
          <p class="event-chart-subtitle"><%= t('event.rooms_load_subtitle') %></p>
        </div>
        <span class="chart-kicker"><%= roomChart.total %> <%= roomChart.total !== 1 ? t('event.rooms_plural') : t('event.rooms_singular') %></span>
      </div>

      <% if (roomChart.items.length === 0) { %>
        <div class="chart-empty"><%= t('event.chart_rooms_empty') %></div>
      <% } else { %>
        <div class="chart-note"><strong><%= roomChart.activeNow %></strong> <%= roomChart.activeNow !== 1 ? t('event.occupied_now_plural') : t('event.occupied_now_singular') %> · <strong><%= roomChart.availableNow %></strong> <%= roomChart.availableNow !== 1 ? t('event.available_plural') : t('event.available_singular') %></div>
        <div class="chart-bars">
          <% roomChart.items.forEach(function(item) { %>
            <div class="chart-bar-item">
              <div class="chart-bar-topline">
                <span class="chart-bar-label"><%= item.name %></span>
                <span class="chart-bar-meta"><%= item.assigned %> <%= item.assigned !== 1 ? t('event.assignments_plural') : t('event.assignments_singular') %></span>
              </div>
              <div class="chart-note"><%= item.matchType %> · <%= item.type %> · <% if (!item.isActive) { %><%= t('status.inactive') %><% } else if (item.active > 0) { %><%= item.active %> <%= item.active !== 1 ? t('event.active_plural') : t('event.active_singular') %><% } else { %><%= t('event.free') %><% } %> · <%= item.done %> <%= item.done !== 1 ? t('event.finished_plural') : t('event.finished_singular') %></div>
              <div class="chart-bar-track">
                <div class="chart-bar-fill" style="width:<%= item.width %>%; background:linear-gradient(90deg, #00897B, #4DB6AC);"></div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </section>
  </div>

  <div class="event-details-grid">

    <!-- Classement -->
    <div class="event-section-card event-ranking-card">
      <h2 style="font-size:1.1rem; margin-bottom: var(--space-md); display:flex; align-items:center; gap:.5rem;">
        <i class="fas fa-ranking-star"></i> <%= t('event.ranking') %>
      </h2>

      <% if (rankings.length === 0) { %>
        <div class="card" style="text-align:center; padding: var(--space-lg); color: var(--color-text-muted);">
          <i class="fas fa-ranking-star" style="font-size:2rem; margin-bottom:.5rem; display:block; opacity:.3;"></i>
          <p style="margin:0;">
            <% if (isDone) { %>
              <%= t('event.no_points_awarded') %>
            <% } else { %>
              <%= t('event.ranking_available_later') %>
            <% } %>
          </p>
        </div>
      <% } else { %>
        <div class="card" style="overflow:hidden;">
          <div class="card-body" style="padding:0;">
            <table style="width:100%; border-collapse:collapse; font-size:.9rem;">
              <thead>
                <tr style="background: rgba(0,0,0,.03);">
                  <th style="padding: var(--space-sm) var(--space-md); text-align:left;">#</th>
                  <th style="padding: var(--space-sm) var(--space-md); text-align:left;"><%= t('event.player') %></th>
                  <th style="padding: var(--space-sm); text-align:right;"><%= t('event.points_short') %></th>
                  <th style="padding: var(--space-sm); text-align:right;"><%= t('event.wins_short') %></th>
                  <th style="padding: var(--space-sm) var(--space-md); text-align:right;"><%= t('battle.battles') %></th>
                </tr>
              </thead>
              <tbody>
                <% rankings.forEach(function(entry) {
                  let medalIcon = '';
                  let medalColor = '';
                  if (entry.rang === 1)      { medalIcon = '🥇'; medalColor = '#FFD700'; }
                  else if (entry.rang === 2) { medalIcon = '🥈'; medalColor = '#C0C0C0'; }
                  else if (entry.rang === 3) { medalIcon = '🥉'; medalColor = '#CD7F32'; }
                %>
                  <tr class="ranking-row-hover" style="border-top:1px solid rgba(0,0,0,.05);">
                    <td style="padding: var(--space-sm) var(--space-md); font-weight:bold; color:<%= medalColor || 'inherit' %>; font-size:<%= entry.rang <= 3 ? '1.1rem' : '.95rem' %>;">
                      <%= medalIcon || entry.rang %>
                    </td>
                    <td style="padding: var(--space-sm) var(--space-md); font-weight:600; color:var(--color-text);">
                      <%= entry.username %>
                    </td>
                    <td style="padding: var(--space-sm); text-align:right;">
                      <span style="background:#E3F2FD; color:#1976D2; padding:.2rem .5rem; border-radius:1rem; font-size:.82rem; font-weight:600;">
                        <%= entry.points %>
                      </span>
                    </td>
                    <td style="padding: var(--space-sm); text-align:right;">
                      <span style="background:#E8F5E9; color:#388E3C; padding:.2rem .5rem; border-radius:1rem; font-size:.82rem; font-weight:600;">
                        <%= entry.wins %>
                      </span>
                    </td>
                    <td style="padding: var(--space-sm) var(--space-md); text-align:right; color:var(--color-text-muted); font-size:.85rem;">
                      <%= entry.battles_played %>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        </div>
      <% } %>
    </div>

    <!-- Rencontres -->
    <div class="event-section-card">
      <h2 style="font-size:1.1rem; margin-bottom: var(--space-md); display:flex; align-items:center; gap:.5rem;">
        <i class="fas fa-gamepad"></i> <%= t('battle.battles') %>
      </h2>

      <% if (battles.length === 0) { %>
        <div class="card" style="text-align:center; padding: var(--space-lg); color: var(--color-text-muted);">
          <i class="fas fa-gamepad" style="font-size:2rem; margin-bottom:.5rem; display:block; opacity:.3;"></i>
          <p style="margin:0;"><%= t('event.no_battle') %></p>
        </div>
      <% } else { %>
        <div class="event-battles-list">
          <% battles.forEach(function(battle) {
            let statusClass = 'battle-status--queue';
            let statusIcon  = 'fas fa-clock';
            let statusLabel = t('status.queue');
            if (battle.status === 'planned')     { statusClass = 'battle-status--planned'; statusIcon = 'fas fa-calendar-check'; statusLabel = t('status.planned'); }
            if (battle.status === 'setup')       { statusClass = 'battle-status--install'; statusIcon = 'fas fa-screwdriver-wrench'; statusLabel = t('status.setup'); }
            if (battle.status === 'in_progress') { statusClass = 'battle-status--live';    statusIcon = 'fas fa-bolt'; statusLabel = t('status.in_progress'); }
            if (battle.status === 'ended')       { statusClass = 'battle-status--done';    statusIcon = 'fas fa-flag-checkered'; statusLabel = t('status.ended'); }
            const winners = (battle.players || []).filter(p => p.is_winner);
            const allPlayers = battle.players || [];
          %>
            <div class="card event-battle-card">
              <div class="event-battle-header">
                <div class="event-battle-body">
                  <div style="display:flex; align-items:center; gap:.5rem; margin-bottom:.25rem; flex-wrap:wrap;">
                    <span class="battle-status-badge <%= statusClass %>">
                      <i class="<%= statusIcon %>"></i> <%= statusLabel %>
                    </span>
                    <span class="event-battle-meta">
                      #<%= battle.id %> · <%= battle.game_name %>
                      <% if (battle.match_type) { %> · <%= battle.match_type %><% } %>
                    </span>
                  </div>

                  <!-- Joueurs -->
                  <% if (allPlayers.length > 0) { %>
                    <div class="event-battle-players">
                      <% allPlayers.forEach(function(p, pi) { %>
                        <span style="color: <%= p.is_winner ? '#388E3C' : 'inherit' %>; font-weight: <%= p.is_winner ? 600 : 'normal' %>;">
                          <%= p.is_winner ? '🏆 ' : '' %><%= p.username %>
                        </span><% if (pi < allPlayers.length - 1) { %> <span style="margin:0 .2rem;">vs</span> <% } %>
                      <% }); %>
                    </div>
                  <% } %>

                  <!-- Score -->
                  <% if (battle.score) { %>
                    <div style="font-size:.8rem; color:var(--color-text-muted); margin-top:.1rem;">
                      <i class="fas fa-trophy" style="font-size:.7rem;"></i> <%= t('event.score') %> : <strong><%= battle.score %></strong>
                    </div>
                  <% } %>
                </div>

                <!-- Salle -->
                <% if (battle.room_name) { %>
                  <div class="event-battle-room">
                    <i class="fas fa-door-open"></i> <%= battle.room_name %>
                  </div>
                <% } %>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </div>

  </div><!-- /grid -->

</div>

<script src="/socket.io/socket.io.js"></script>
<script>
/* ── Compte à rebours ── */
(function () {
  'use strict';
  function pad(n) { return String(n).padStart(2, '0'); }

  document.querySelectorAll('.event-countdown--sm').forEach(function (el) {
    const deadline = new Date(el.dataset.deadline).getTime();
    const nums = el.querySelectorAll('.countdown-num');
    if (nums.length < 4) return;

    function tick() {
      const diff = deadline - Date.now();
      if (diff <= 0) {
        el.closest('.event-countdown-wrap').innerHTML =
          '<p style="font-size:.85rem;"><i class="fas fa-lock"></i> <%= t('event.registrations_closed') %></p>';
        return;
      }
      nums[0].textContent = Math.floor(diff / 86400000);
      nums[1].textContent = pad(Math.floor((diff % 86400000) / 3600000));
      nums[2].textContent = pad(Math.floor((diff % 3600000)  / 60000));
      nums[3].textContent = pad(Math.floor((diff % 60000)    / 1000));
      setTimeout(tick, 1000);
    }
    tick();
  });

  /* ── Socket.IO — rechargement auto sur mise à jour du classement ── */
  if (typeof window.io === 'function') {
    const socket = window.io({ path: '/socket.io', transports: ['websocket', 'polling'] });
    let reloadTimer = null;

    socket.on('connect', function () {
      socket.emit('ranking:join-all');
    });

    socket.on('ranking:update', function () {
      if (reloadTimer) window.clearTimeout(reloadTimer);
      reloadTimer = window.setTimeout(function () {
        window.location.reload();
      }, 500);
    });
  }
}());
</script>

<%- include('../partials/foot') %>
