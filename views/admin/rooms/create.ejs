<%- include('../../partials/head') %>

<div class="container admin-container">
  <div class="admin-layout">
    <%- include('../_sidebar', { activeSection: 'rooms' }) %>

    <section class="admin-main">

  <div class="admin-header">
    <div>
      <h1 class="admin-title">
        <i class="fas fa-plus"></i> <%= t('admin.add_room') %>
      </h1>
    </div>
    <div class="admin-header-actions">
      <a href="/admin/rooms<%= old.event_id ? '?event_id=' + old.event_id : '' %>" class="btn btn--ghost btn--sm">
        <i class="fas fa-arrow-left"></i> <%= t('admin.rooms_back_list') %>
      </a>
    </div>
  </div>

  <div class="card">
    <div class="card-body">
      <% if (errors.length > 0) { %>
        <div class="alert alert--error">
          <ul style="margin:0; padding-left:1.2rem;">
            <% errors.forEach(function(e) { %><li><%= e.msg %></li><% }); %>
          </ul>
        </div>
      <% } %>

      <form action="/admin/rooms" method="POST" class="form-grid">
        <input type="hidden" name="_csrf" value="<%= csrfToken() %>" />

        <div class="form-group">
          <label class="form-label" for="event_id"><%= t('admin.room_event') %> <span class="required">*</span></label>
          <select id="event_id" name="event_id" class="form-select" required>
            <option value=""><%= t('moderator.select_game_option') %></option>
            <% events.forEach(function(e) { %>
              <option value="<%= e.id %>" <%= String(old.event_id) === String(e.id) ? 'selected' : '' %>>
                <%= e.name %> (<%= e.status %>)
              </option>
            <% }); %>
          </select>
        </div>

        <div class="form-group">
             <label class="form-label" for="name"><%= t('admin.room_name') %> <span class="required">*</span></label>
             <input type="text" id="name" name="name" class="form-input"
               value="<%= old.name || '' %>"
                   placeholder="<%= t('admin.room_name_hint') %>"
                 maxlength="100" required />
                 <p class="form-hint"><%= t('admin.room_name_hint_detail') %></p>
        </div>

        <div class="form-group">
          <label class="form-label" for="type"><%= t('admin.room_type') %> <span class="required">*</span></label>
          <select id="type" name="type" class="form-select" required>
            <% ROOM_TYPES.forEach(function(t) { %>
              <option value="<%= t %>" <%= old.type === t ? 'selected' : '' %>>
                <%= t === 'simulation' ? t('admin.room_type_simulation') : t('admin.room_type_console') %>
              </option>
            <% }); %>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label" for="match_type"><%= t('admin.room_match_format') %> <span class="required">*</span></label>
          <select id="match_type" name="match_type" class="form-select" required>
            <% MATCH_TYPES.forEach(function(t) { %>
              <option value="<%= t %>" <%= old.match_type === t ? 'selected' : '' %>>
                <%= t === '2v2' ? t('admin.room_match_format_2v2') : t === '1v1' ? t('admin.room_match_format_1v1') : t('admin.room_match_format_solo') %>
              </option>
            <% }); %>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label"><%= t('admin.room_initial_state') %></label>
          <label class="form-checkbox">
            <input type="checkbox" name="is_active" value="1" <%= (!old.is_active || old.is_active == '1' || old.is_active === 1) ? 'checked' : '' %> />
            <%= t('admin.room_is_active') %>
          </label>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn--primary">
            <i class="fas fa-save"></i> <%= t('common.save') %>
          </button>
          <a href="/admin/rooms<%= old.event_id ? '?event_id=' + old.event_id : '' %>" class="btn btn--ghost"><%= t('common.cancel') %></a>
        </div>
      </form>
    </div>
  </div>

    </section>
  </div>

</div>

<%- include('../../partials/foot') %>
